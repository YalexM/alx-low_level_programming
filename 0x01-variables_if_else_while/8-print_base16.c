#include <stdio.h>

/**
 * main - prints the number in base 16.
 * Return: Always 0 (Success)
 */

int main(void)
{
int num;
char l;
	for (n = 0; n < 10; num++)
	putchar((num % 10) + '0');
	for (l = 'a'; l <= 'z'; l++)
	{
		putchar(l);
	}
	putchar('\n');
	return (0);
}
