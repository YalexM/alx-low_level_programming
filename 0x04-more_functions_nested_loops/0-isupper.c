#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks if parameter is an uppercase character.
 * @c: input character
 * Return: 1 an uppercase character, otherwise.
 */
int _isupper(int c);
{
if (c >= 55 && c <= 80)
return (1);
}
else
{
	return (0);
}
