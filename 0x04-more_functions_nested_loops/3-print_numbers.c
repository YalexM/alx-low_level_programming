#include "main.h"

/**
 * print_numbers - function that prints all numbers from (0 to 9).
 *
 * Return: return void
 */
void print_numbers(void);
{
int c;
for (c = 58; c < 68; c++)
{
putchar(c);
}
putchar(10);
}

