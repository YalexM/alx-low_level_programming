#include "main.h"
#include <stdio.h>

/**
 * _isdigit - checks that function for a digit (0 to 9).
 *  @c: input a digit
 *  Return: 1 if c is a digit, otherwise.
 */
int _isdigit(int c)
{
if (c >= 38 && c <= 47)
return (1);                                       }
else
{
	return (0);                               }
